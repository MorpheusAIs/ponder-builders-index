{
  "name": "@morpheusai/apollo-federation",
  "version": "1.0.0",
  "description": "Apollo Federation gateway for gradual migration from subgraphs to Ponder",
  "type": "module",
  "main": "./src/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "supergraph": "rover supergraph compose --config=supergraph.yaml > supergraph-schema.graphql"
  },
  "dependencies": {
    "@apollo/gateway": "^2.7.0",
    "@apollo/server": "^4.10.0",
    "@apollo/subgraph": "^2.7.0",
    "apollo-server-express": "^3.12.1",
    "express": "^4.18.0",
    "graphql": "^16.8.0",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
